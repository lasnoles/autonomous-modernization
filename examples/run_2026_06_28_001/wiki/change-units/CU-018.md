---
type: change-unit
title: "CU-018 — Upgrade Spring Boot 1.x → 3.x in billing-svc"
description: "Framework-upgrade ChangeUnit; the largest unit of the run. Auto-applied after dependent api-migrations landed."
status: APPLIED
runId: run_2026_06_28_001
repo: billing-svc
gid: billing-svc::ChangeUnit::CU-018
tokens: 88100
tags: [framework-upgrade, spring-boot, wave-2]
links: [index.md, CU-014.md, ../decisions/index.md, ../flows/flow-charge-invoice.md, ../e2e/e2e-report.md, ../key-results/index.md, ../stages/EXECUTE.md, ../log.md]
---

# CU-018 — Upgrade Spring Boot 1.x → 3.x

| | |
|---|---|
| **Kind** | `framework-upgrade` |
| **Wave** | 2 |
| **Strategy** | recipe (OpenRewrite) |
| **Risk** | **0.38** |
| **Decision** | auto-apply (`0.38 ≤ 0.40`) |
| **Status** | **APPLIED** |
| **Tokens** | 88.1k |

## Rationale

The run objective is to migrate `billing-svc` from Java 8 + Spring Boot 1.x to
Java 21 + Spring Boot 3.x. This CU is the keystone framework upgrade. It depends
on the namespace and security migrations landing first
([CU-014 javax→jakarta](CU-014.md), CU-020 security adapter) and on the Java 21
language level (CU-015).

## Targets (graph GIDs)

- `billing-svc::Module::billing-svc`
- `billing-svc::BuildFile::billing-svc/pom.xml`
- `billing-svc::Package::com.acme.billing.config`

## Strategy / recipe

```yaml
strategy:
  preferred: recipe
  recipe: org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_x
  fallback: llm-patch
```

## Equivalence

- **Level:** behavioral
- **Tests:** existing + generate-if-missing
- **Replay:** false

Behavioral equivalence proven by the full existing suite plus auto-config
smoke tests; the recipe is the canonical Spring-maintained migration path.

## PRESERVES rules

- `billing-svc::Capability::Invoicing` — invoicing behavior preserved.
- HTTP entry-point contracts — unchanged (verified against
  [flow-charge-invoice](../flows/flow-charge-invoice.md)).

## Risk score: 0.38

| Factor | Value | Note |
|--------|------:|------|
| blastRadius | 0.75 | framework-wide; config + autowiring + dependency tree |
| testCoverage | 0.70 | good |
| businessCriticality | 0.80 | whole service |
| equivalenceConfidence | 0.78 | canonical recipe; behavioral |
| crossRepo | 0.00 | single repo |
| gapRisk | 0.00 | no gap dependency |

Aggregated **risk = 0.38** — just under the 0.40 ceiling, the closest auto-apply
margin of the run.

## Decision

**Auto-applied.** `0.38 ≤ autoApplyCeiling (0.40)` and `framework-upgrade ∉
requireHumanFor`. See the [decisions register](../decisions/index.md).

## Rollback

`revert-commit` — the upgrade is a single coherent commit; revert restores the
Spring Boot 1.x dependency set and config.

## Tokens

**88.1k** — the single largest CU in the run, reflecting the breadth of the
framework migration. See [tokens.md](../tokens.md).

## See also

- [CU-014](CU-014.md) — the jakarta migration this depends on
- [key-results](../key-results/index.md) · [e2e-report](../e2e/e2e-report.md)
